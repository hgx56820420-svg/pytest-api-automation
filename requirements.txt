# 被测服务（app/）依赖
#
# 版本说明：本机 Python 3.14，pydantic-core 必须 >= 2.41 才有 cp314 预编译轮子，
# 否则 pip 会尝试从源码编译（需要 Rust 工具链），在没网/没 Rust 的机器上直接失败。
# 所以这里的 pydantic 不能降到 2.12 以下。
fastapi==0.141.1
uvicorn[standard]==0.52.1
sqlalchemy==2.0.51
pydantic==2.13.4
PyJWT==2.10.1

# 测试（tests/）依赖
pytest==9.1.1
requests==2.34.2
jsonschema==4.26.0
Faker==40.36.0
allure-pytest==2.16.0

# V2 多 Agent 工作流编排（LangGraph 用于有分支的工作流；节点目前为确定性函数，
# 后续接入 LLM 时在节点内部使用 LangChain 结构化调用即可，图结构不变）
langgraph==1.2.11
# 需求拆解 Agent 的 LLM 结构化调用（可选功能：--llm-analysis + .env 配置）
langchain-openai==1.6.2
