# 设计决策

## D001：取消订单验证真实副作用

取消订单不仅要返回 `cancelled`，还必须断言库存和余额恢复到下单前的值。这样可以验证事务回滚和业务副作用，而不是只验证响应体。

## D002：错误信息读取 `detail`

服务使用 FastAPI `HTTPException(detail=...)`。测试统一读取 `resp.json().get("detail")`，不使用不存在的 `message` 字段。

## D003：按实际复用程度设计 fixture

固定商品 fixture 用于多数订单测试；只被单个余额不足场景使用的高价商品直接在测试内创建，避免为一次性数据增加长期维护成本。

## D004：动态状态与历史文档分离

`STATUS.yaml` 只描述当前事实和下一步；长篇交接文档保留背景、过程和教学记录。新会话先读动态状态，再按需检索历史。
