# 交接文档 — pytest-api-automation

> 最后更新：2026-08-11
> 换机器后，把这份文档从头读一遍即可无缝接上。

---

## 一、这个项目是什么

**目标**：做一个求职面试用的作品集项目——Python + pytest 的接口自动化测试框架。

**关键约束（很重要，别忘）**：
- 作品的价值不在代码量，在**你能讲清楚每一层为什么这么设计**
- 所以协作方式是：AI 讲原理 + 给骨架 + code review，**测试代码由你本人写**
- 唯一例外：被测服务（`app/`）由 AI 写，因为它不是学习目标，你只需读懂

**仓库**：https://github.com/hgx56820420-svg/pytest-api-automation

---

## 二、6 个阶段的路线图（当前进度：阶段 4 进行中）

| 阶段 | 内容 | 谁写 | 状态 |
|---|---|---|---|
| 1 | FastAPI 被测服务（靶子） | AI | 已完成 |
| 2 | 裸写第一个测试（故意写丑，体会痛点） | **你** | 已完成 |
| 3 | 封装 HTTP 客户端 + conftest fixture + 多环境配置 | **你** | 已完成 |
| 4 | 数据驱动（parametrize）+ 响应结构校验 + faker 造数 | **你** | 进行中（见下方"当前进度"） |
| 5 | Allure 报告 + GitHub Actions CI | **你** | 未开始 |
| 6 | README（含架构图和设计决策说明） | **你** | 未开始 |

---

## 三、当前进度（阶段 4，2026-08-11）

### 已完成并验证通过的内容

全套 **16 条测试全部通过**（`pytest tests/ -v` → `16 passed`）：

**`tests/test_auth.py`（8 条）**：
- `test_register_success` 用 `@pytest.mark.parametrize` 对 username 长度做边界值测试
  (2/3/20/21 位 → 422/201/201/422)，直接对应 `app/schemas.py` 的 `min_length=3, max_length=20`
- `test_register_duplicate_username`、`test_login_success`、`test_login_wrong_password`、
  `test_me_without_token` 复用 `conftest.py` 里的 `base_url` / `registered_user` fixture
- `test_login_success` 用 `jsonschema` 的 `TOKEN_SCHEMA` 校验响应结构

**`tests/test_products.py`（6 条）**：
- `test_create_product_success`（用 faker 造 name/price/stock）
- `test_create_product_without_token` → 401
- `test_get_product_not_found` → 404（路径参数场景）
- `test_create_product_invalid_price`（price=0）→ 422
- `test_list_products_invalid_pagination` 参数化覆盖 page=-1 和 size=1000 → 422

**`tests/test_order.py`（2 条）**：
- `test_cancel_order_restores_stock_and_balance`（最有价值的一条）
  下单 → 取消 → 重新查询商品库存和用户余额 → 断言**副作用真的回滚**
  这是整个项目"分水岭"级别的用例，绝大多数候选人只断言响应体，
  这条能断言接口的副作用（库存和余额恢复到下单前的值）
- `test_create_order_success`
  下单基础成功路径，断言 `status == "created"`、`amount == 20.0`（10.0 单价 × 2 数量）、
  `product_id` 对得上。**注意**：这条一开始误照抄了副作用测试的结构（多查了初始库存/余额），
  已经精简成只发一次下单请求 + 三条断言——每条测试只做跟自己意图相关的动作，这是本轮的关键教训。

### `conftest.py` 里已有的 fixture

按依赖链排列：

```
base_url                    读环境变量 API_BASE_URL，默认 127.0.0.1:8010
  └── registered_user       注册一个新用户，返回 {username, password}
        └── auth_token      登录拿 access_token 字符串
              └── auth_headers    返回 {"Authorization": "Bearer xxx"} 字典
                    └── created_product    创建一个固定 price=10、stock=5 的商品
```

还有一份 `TOKEN_SCHEMA`（jsonschema 格式的登录响应结构定义）。

### 已装并写入 `requirements.txt` 的测试依赖

`pytest==9.1.1`、`requests==2.34.2`、`jsonschema==4.26.0`、`Faker==40.36.0`

### 额外做的事：`app/models.py` 补全了详细注释

不属于测试代码本身，但为了帮助理解被测服务，给 `app/models.py`（User/Product/Order 三张表）
补上了逐字段的注释，解释了：
- ORM 是什么、`Mapped`/`mapped_column` 语法在做什么
- 为什么密码存哈希不存明文
- `unique=True`/`index=True`/`ForeignKey` 各自的作用
- Order 表的状态机（created → paid / created → cancelled，且都是终态不能反悔）
- `amount` 为什么要在下单时存一份快照，而不是每次现算

这份注释对读懂被测服务、以及面试时讲清楚"数据模型为什么这么设计"会有帮助。

---

## 四、阶段 4 剩余的完整范围（下次打开电脑继续往下推）

订单模块目前写了 2 条（下单成功 + 副作用回滚）。参考 `HANDOFF.md` 第五节
"服务里刻意埋好的测试点"和 `app/routers/orders.py` 的业务逻辑，订单模块还要覆盖：

- **库存不足** → 400（比如 stock=5 时下单 quantity=10）
- **余额不足** → 400（需要造一个高价商品让总价超过 1000）
- **商品不存在** → 404（product_id=999999）
- **quantity 非法** → 422（quantity=0 或负数，看 `OrderCreateRequest` 的 `gt=0, le=100`）
- **状态机非法流转**：
  - 已支付的订单再 pay → 409
  - 已取消的订单再 cancel → 409
  - 已支付的订单 cancel → 409（只有 created 状态才能 cancel）
- **越权访问**：用 A 的 token 查 B 的订单 → 404（服务端不泄露资源是否存在）

**下一步具体要做什么**：从"库存不足 → 400"开始。这条会用到 `created_product`
（固定 stock=5），下单时传 quantity=10（超过库存），断言 400。写法思路跟
`test_create_order_success` 类似，只是断言状态码不同、请求体的 quantity 故意超库存。

这部分具体怎么拆分、要不要全部覆盖，做完后再讨论。
如果觉得订单模块已经覆盖了核心场景，也可以**直接跳到阶段 5（Allure 报告 + CI）**，
回头再补订单用例也行——你定。

---

## 五、换机器后的环境准备

### 1. 拉代码

```powershell
git clone https://github.com/hgx56820420-svg/pytest-api-automation.git
cd pytest-api-automation
```

### 2. 建虚拟环境并装依赖

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 3. 启动被测服务

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8010
```

启动后打开 http://127.0.0.1:8010/docs 能看到 Swagger UI 就算成功。

### 4. 跑一遍测试确认环境没问题

新开一个终端：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/ -v
```

预期 `16 passed`。

---

## 六、环境上的坑（都是已经踩过的，别重复踩）

1. **PowerShell 5.1 不支持 `&&`**
   多条命令用 `;` 分隔。

2. **端口 8000 在原来那台机器上被别的程序占用了**
   所以本项目统一用 **8010**。症状是 uvicorn 报 `[Errno 10048]`。

3. **Python 3.14 + pydantic 版本问题**
   `requirements.txt` 已经锁到 `pydantic==2.13.4`（有 cp314 预编译包），**不要往下降版本**。

4. **密码哈希用的是标准库 pbkdf2，不是 bcrypt**
   刻意的选择，避免 Windows 上编译踩坑。

5. **VS Code 没开自动保存会导致 pytest 显示 `no tests ran`**
   `Ctrl+Shift+P` → `Preferences: Open Settings (UI)` → 搜 `files.autoSave` → 选 `afterDelay`

---

## 七、被测服务代码导读（阶段 1 产物）

```
app/
├── config.py      配置（JWT 密钥、token 有效期、数据库地址、初始余额）
├── database.py    SQLAlchemy engine / session / get_db 依赖
├── models.py      三张表：User / Product / Order
├── schemas.py     请求与响应的 Pydantic 契约（参数校验规则都在这里）
├── auth.py        密码哈希 + JWT 签发/校验 + get_current_user 依赖
├── main.py        服务入口，挂载三个路由 + /health
└── routers/
    ├── auth.py        注册 / 登录 / 查当前用户
    ├── products.py    商品列表 / 详情 / 创建 / 更新 / 下架
    └── orders.py      下单 / 我的订单 / 详情 / 支付 / 取消
```

### 14 个接口清单

**认证** `/api/auth`
- `POST /register` — 201；用户名重复 409；username<3位 或 password<6位 → 422
- `POST /login` — 200 返回 access_token；账号或密码错 → 401
- `GET /me` — 需 Token；无/错/过期 Token → 401

**商品** `/api/products`
- `GET ""` — 分页列表，支持 `page`/`size`/`keyword`/`status`
- `GET /{id}` — 详情；不存在 404
- `POST ""` — 201，需 Token
- `PUT /{id}` — 部分更新（只改传了的字段）
- `DELETE /{id}` — 软删除，status 改成 off_sale，记录不消失

**订单** `/api/orders`
- `POST ""` — 201；商品不存在 404；已下架/库存不足/余额不足 → 400；quantity<=0 → 422
- `GET ""` — 我的订单列表，支持 status 筛选
- `GET /{id}` — 详情；别人的订单也返回 404（不泄露存在性）
- `POST /{id}/pay` — created→paid；非 created 状态 → 409
- `POST /{id}/cancel` — created→cancelled，**并回滚库存和余额**；非 created → 409

**其他**
- `GET /health` — 给 CI 轮询等待服务就绪用

### 服务里刻意埋好的测试点

- **鉴权链路**：不带 token / 格式错 / 签名错 / 过期 / 用户已删 → 全部 401
- **库存边界**：stock=1 时下单 2 个 → 400
- **余额边界**：余额刚好等于金额 → 应该成功；差 0.01 → 400
- **状态机非法流转**：已支付的订单再取消 → 409
- **参数校验**：`page=0`、`size=-1`、`size=101`、`quantity=0` → 422
- **越权访问**：用 A 的 token 查 B 的订单 → 404
- **副作用与回滚**（最有价值的一类）：
  取消订单后要断言**库存回到原值、余额回到原值**，而不只是断言响应体的 status 字段。

---

## 八、给新会话 AI 的提示词

直接复制粘贴（也在 `HANDOFF_PROMPT.md` 里）：

```
我在做一个 pytest 接口自动化测试的求职作品集项目，仓库已经 clone 到本地。
请先读 交接文档.md 和 question.md 了解背景。

关键协作原则（请严格遵守）：
- 测试代码必须由我自己写，你只负责讲原理、给思路、review 我写的代码、
  指出错误在哪里和为什么错——不要直接把完整代码写好发给我
- 被测服务（app/ 目录下的 FastAPI 代码）是你之前写的，不用再改
- 每完成一个小块功能，帮我验证测试真的跑通，并且做"故意让它失败"的反向验证
- Windows + PowerShell 5.1，不支持 `&&`，多条命令用 `;` 分隔
- 服务用 8010 端口：.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8010

请先读完两份文档，用 2-3 句话总结当前进度，然后告诉我下一步该做什么。
```

---

## 九、Git 操作备忘

```powershell
git add -A
git commit -m "描述做了什么"
git push
```

约定：**每个阶段至少一个独立 commit**。
git 历史本身就是你成长过程的证据，面试官会看提交记录。

远端已配好（`origin` → pytest-api-automation.git），上游跟踪已建立，
`git push` 不需要再带参数。
